package common

templ Button(label string, class string) {
	<button class={ "bg-blue-700 hover:bg-blue-900 text-white font-bold py-2 px-4 rounded-lg focus:outline-none focus:shadow-outline m-6", class }>{ label }</button>
}
