package common

templ Typography(variant string, label string, classNames string) {
	if variant == "header" {
		<h1 class={ "text-red-600 font-sans text-xl", classNames }>{ label }</h1>
	} else {
		<p class={ "text-red-600 font-sans text-sm", classNames }>{ label }</p>
	}
}
